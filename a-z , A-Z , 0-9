#include <iostream>
using namespace std;
int main() {
    string a;
    cin>>a;
    int flag1=0,flag2=0,flag3=0;
    for(int letter=0;i<a.length();i++){
        flag1 = flag1 | (1<<(a[letter]-'a'));
        flag2 = flag2 | (1<<(a[letter]-'A'));
        if(a[letter]>='0' && a[letter]<='9'){
flag3 = flag3 | (1<<(a[letter]-'0'));
    }
    cout<<(((flag2 == (1<<26)-1)  && (flag2 == (1<<26)-1) &&(flag3 ==(1<<10)-1))?"yes":"no");
    return 0;
}
